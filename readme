*2022.04.09 上海自03.28封城已经12天 但我们居民大多在03.18左右就居家隔离 不准外出
 搭建环境遇到的问题:
    1.Goland编译器中External Libraries的GOPATH一直是空的 确定了GOPATH引入的路径是对的 并且使用的是GOPATH而不是GOMODULE
    最后发现问题是GOPATH路径中也需要bin pkg src这三个文件夹 新建文件夹后 External Libraries有了内容
    2.cd进入GOPATH\src后 git clone xxx 拉取需要引用的扩展包 仍提示无法找到该扩展文件
    最后发现问题是src路径下需要特定的文件名 github.com golang.org 之后下载扩展最好使用go get 带有完整的路径 不是扩展里的某个文件夹
    3.如果go get拉取的扩展包不在GOPATH路径下 则是GOPATH路径设置的问题
 新增日志模块 log文件下相关代码

*2022.04.10 上海居家隔离的一个周末 今天没有抗原检测 也没有核酸检测
 升级golang版本遇到的问题:
    1.升级后goland中引入GOROOT提示The selected directory is not a valid home for Go SDK
    解决办法是在GOPATH\src\runtime\internal\sys\zversion.go文件中添加一行版本信息`const TheVersion = `go1.17.6``
    2.升级后goland中莫名报很多的错误 但是不影响编译和运行
    是因为golang的版本和goland不兼容 升级goland为最新后 报错消失
 新增数据库模块 目前仅支持连接一个数据库

*2022.04.13 上海今天下了一天的暴雨 现在仍在继续 另外今天做了两次抗原检测 都是正常的
 完善数据库模块 支持连接多个数据库 但是目前尚不支持自动建表